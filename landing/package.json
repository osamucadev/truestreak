{
  "name": "truestreak",
  "version": "1.0.0",
  "description": "Professional landing page showcasing React and React Native expertise",
  "scripts": {
    "clean": "node scripts/clean.js",
    "build:scss": "node scripts/build-scss.js",
    "build:js": "node scripts/build-js.js",
    "optimize:images": "node scripts/optimize-images.js",
    "hash:assets": "node scripts/hash-assets.js",
    "build:html": "node scripts/build-html.js",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "generate:robots": "node scripts/generate-robots.js",
    "build": "npm run clean && npm run build:scss && npm run build:js && npm run optimize:images && npm run hash:assets && npm run build:html && npm run generate:sitemap && npm run generate:robots",
    "watch:scss": "node scripts/watch-scss.js",
    "watch:js": "node scripts/watch-js.js",
    "watch:html": "node scripts/watch-html.js",
    "watch:assets": "node scripts/watch-assets.js",
    "serve": "firebase serve --port 5000",
    "browser-sync": "browser-sync start --proxy \"localhost:5005\" --files \"public/**/*\" --no-notify --no-open",
    "dev": "concurrently --kill-others --names \"SCSS,JS,HTML,ASSETS,FIREBASE,BROWSER\" -c \"yellow,blue,green,magenta,cyan,red\" \"npm run watch:scss\" \"npm run watch:js\" \"npm run watch:html\" \"npm run watch:assets\" \"npm run serve\" \"npm run browser-sync\"",
    "start": "npm run build && npm run dev",
    "deploy": "npm run build && firebase deploy --only hosting:truestreak-landing"
  },
  "keywords": [
    "portfolio",
    "react",
    "react-native",
    "frontend",
    "developer"
  ],
  "author": "Samuel Caetité",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "chokidar": "^3.6.0",
    "concurrently": "^8.2.2",
    "crypto": "^1.0.1",
    "esbuild": "^0.20.0",
    "html-minifier-terser": "^7.2.0",
    "sass": "^1.77.0",
    "sharp": "^0.33.5"
  }
}
